# Loan Approval Prediction System

A Machine Learning project for predicting loan approval using **Logistic Regression, Decision Tree, and Random Forest**.

This repository contains multiple implementations of the same project:

* **Simple Python Version**
* **Flask Web Application**
* **Streamlit Web Application**

The project includes **data cleaning, preprocessing, EDA, model training, evaluation, and prediction**.
